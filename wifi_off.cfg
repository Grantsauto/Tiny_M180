[gcode_macro WIFI_OFF]
gcode:
    {% if printer.has_ethernet() and printer.ethernet_connected() %}
        EXECUTE M303 E-1 P3 S"sudo ifconfig wlan0 down"
    {% endif %}

[gcode_macro WIFI_OFF]
default_parameter_P: 3
